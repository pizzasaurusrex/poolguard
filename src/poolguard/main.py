"""Entry point. Pipeline wiring lands in P2; P0/P1 use the benchmark and
recording scripts."""


def main() -> None:
    raise SystemExit(
        "poolguard pipeline not implemented yet (P2). "
        "For Phase 0, run: poolguard-benchmark --rtsp-url <url>"
    )


if __name__ == "__main__":
    main()
