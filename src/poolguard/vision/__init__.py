"""Vision layer: frame acquisition and pose estimation behind swappable seams.

Modules in this package may require the `vision` extra (opencv, ultralytics);
protocol and model definitions in `frames` and `pose` stay dependency-light so
the rest of the pipeline can import them everywhere.
"""
